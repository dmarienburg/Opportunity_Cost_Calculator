class GatherData:
    """
    Gather CPI (consumer price index) and median income increase data return this data to the requesting method.
    Use the BLS (Bureau of Labor and Statistics) API to do this
    """
