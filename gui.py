


class Gui:
    """
    This class will provide the ipython interface with drop downs, input fields, and an output graph.
    """
    # Create fields to enter: income, current debt, program cost per year, program duration (years),
    # income while in program, income once employed in desired field
    # Create area to display the graph
    # create instances of calculator.CalculateDataTuple() for current_income, while_in_program, after_exiting_program
    # button to loop through the CalculateDataTuple() instances until the current_total_income = proposed_total_income
    # graph the resulting data monthly (one month per tuple)
    # display the months & years until break even below the graph
    pass
